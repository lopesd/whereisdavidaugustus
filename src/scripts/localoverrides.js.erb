async function postPeep(checkinId, peeper, comment) {
  await sleep(1000)
  return { peep: { checkinId, peepAllowed: true, peeper: 'test' } }
}

async function getPeep(checkinId) {
  await sleep(1000)
  return { peep: { checkinId, peepAllowed: true } }
}

console.log('Loaded local overrides.')